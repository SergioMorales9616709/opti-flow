include: package:very_good_analysis/analysis_options.yaml

linter:
  rules:
    # Not applicable to app code (only relevant for published packages/libraries)
    public_member_api_docs: false
    # Repository pattern is architecturally valid with a single method
    one_member_abstracts: false
